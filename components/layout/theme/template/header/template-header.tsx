import type { AlbumDataProps } from '~/types/props.ts'

/**
 * 这个是相册 header 模板，需要写新主题时直接复制一份，然后开写！
 * @param props 组件参数
 */
export default async function DefaultHeader(props: Readonly<AlbumDataProps>) {
  return (
    <>
    </>
  )
}